# Security Check

See the root `SECURITY-CHECK.md` for the latest packaging security summary.

Core rule: the release zip excludes real `sites/` data, `.env`, outputs, logs, runtime files, `node_modules`, and `.git`.
